/*
 * File 5-print_numbers.c
 */

#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0
 *
 * Return: Always 0
 */
int main(void)
{
	int mk;

	for (mk = 0; mk < 10; mk++)
		printf("%d\n", mk);

	return (0);
}
