#include <stdio.h>

/**
 * main -prints all single digit numbers of base 10 starting from 0
 *
 * return always 0
 */
int main(void)
{
	int mk;

	for (mk = 0; mk < 10; mk++)
		printf("%d\n", mk);

	return (0);
}
